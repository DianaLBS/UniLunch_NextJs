import React from "react";
import RestaurantDashboard from '@/components/dashboard/RestaurantDashboard'; // Add missing import statement

const RestaurantPage: React.FC = () => {
    return (
        <div>
            <RestaurantDashboard/>
        </div>
    );
};

export default RestaurantPage;